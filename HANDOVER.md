# HANDOVER

## Session 2026-07-06 (retroactive baseline — onboarding into the Build Org)

Done: Onboarded an already-shipped project into the org's governance. The app itself (v6 → v7 → v8) was built and iterated before the Build Org existed, entirely outside this process — this entry documents that pre-org history as a single baseline rather than reconstructing session-by-session handovers that were never written. Concretely this session: cloned the real `gmanthenoxus/shift-planner` GitHub repo into its fixed home at `/Users/noxus/Builds/Shift Planner/` (previously the app lived only as versioned files in `Downloads/`, with the actual git history only on GitHub); wrote a retroactive `SCOPE.md` capturing what v8 actually, observably does as its frozen baseline; had the `architect` agent reverse-engineer `ARCHITECTURE.md` from the shipped code; added this project's `CLAUDE.md` from the org's standing-orders template; confirmed the live GitHub Pages config (legacy branch-deploy from `main:/`) is correct for a no-build-step static file, not a misconfiguration.

Assumed: That "run it through the build process" meant retroactive onboarding first (governance files matching current shipped behavior, no functional changes), with a follow-on v9 cycle to incorporate recommended changes and the human's own feedback — confirmed with the human before proceeding. That the pre-org commit history (`Add shift planner app`, `Update to v8`, `Add README`) is complete and no other version of this app exists anywhere else undiscovered.

Risky: `ARCHITECTURE.md`'s decisions log flags real, unconfirmed items in the shipped code: the tax-rate calculation uses the user's working *ceiling* (max days × max hours) as its annualizing basis rather than *typical* entered hours, which materially changes the net/hr shown in countries with progressive brackets; the one-off goal hint text ("only while it runs") implies automatic expiry that the code doesn't actually implement (goals run until manually deleted); import only checks that a file parses as JSON, not that it has the right shape, so a malformed-but-parseable import could break rendering; the hardcoded per-country tax brackets will drift out of date over calendar years with no update mechanism. None of these are new bugs introduced this session — they're pre-existing behavior now surfaced and documented for the first time.

Open: The four items above need the human's confirmation (see `ARCHITECTURE.md`'s own Open field for the full list) before they're treated as settled rather than candidate fixes. Separately, a v9 SCOPE.md is expected next, combining a recommended-changes pass against this codebase with feedback the human is gathering from actual use — not yet started as of this entry.

Touched: `SCOPE.md` (created), `ARCHITECTURE.md` (created, by the architect agent), `HANDOVER.md` (created, this entry), `CLAUDE.md` (created, standing orders template). `index.html` and `README.md` were not modified — this session added governance, no functional changes.

## Session 2026-07-06 (v9 build — all 6 SCOPE features implemented)

Done: Implemented SCOPE.md v9 in full against the revised `ARCHITECTURE.md`. Feature 1 (tax corrections): rewrote the entire `COUNTRIES` object against `TAX-ACCURACY-AUDIT.md`'s figures. While implementing, independently discovered and fixed a second structural bug beyond what the audit caught: Australia's bracket array had the exact same shifted-row defect as France (confirmed already in the audit) — hand-traced both before writing any fix, verified in Node against known-correct hand calculations for all 9 countries before touching the browser. Feature 2 (tax basis): `annual` and `annualHours` in `model()` now both derive from `totH` (summed typical hours) instead of `maxWeekly` (the working ceiling); verified live in-browser that the take-home/hr figure matches hand calculation exactly. Feature 3 (Infinity fix): gated the entire headline/baseline/goalRows/reality-bar rendering block on `m.net>0`, with a real empty-state message when it isn't, rather than patching each display point individually — this is the actual fix, not a workaround, since every hours figure downstream of `net` is only guaranteed finite when `net>0`. Also separately guarded the reality bar's percentage math against `maxWeekly=0` (a second divide-by-zero source the SCOPE criterion's wording implied but didn't name explicitly). Feature 4 (goal auto-expiry): added `addedAt` (epoch ms) to goal objects, computed elapsed-weeks-based expiry in `model()`, excluded expired goals from `goalHrsWkNow` and `weekCoverage()`'s waterfall, added an "Expired" pill in both the goals list and goal-rows panel, backfilled `addedAt` on load and on import for pre-v9 data. Feature 5 (import hardening): added `validShape()` checking all five array fields plus `settings`'s presence/type; bad imports now show an inline message and leave `S` untouched; removed both blocking `alert()`s (`newWeek` on zero shifts is now a silent no-op relying on the pre-existing "No shifts logged" empty state; bad import shows inline via a new `#importMsg` element). Feature 6 (a11y/touch): replaced all four icon glyphs (✕/↓/↑/↻) with inline Lucide-sourced SVG paths plus `aria-label`s, added `:focus-visible` styles to `.btn`/`.x`, gave `.x` a 44×44px minimum, added a visible label to the job-role field.

Assumed: Per `ARCHITECTURE.md`'s own Assumed field, confirmed by the human before this session started: inline SVG icons (not a CDN/npm dependency), epoch-ms timestamps for `addedAt`, reject-entire-import on bad shape. Beyond that: (1) the trash icon (not a bare "✕") for all four delete buttons, and the "Expired" pill reusing the existing `.pill`/`--warn` visual language, per `ARCHITECTURE.md`'s own noted assumptions — no objection raised. (2) The Medicare-levy/USC/etc. "stale figure" corrections were applied exactly as `TAX-ACCURACY-AUDIT.md` specified, without re-deriving them independently (the audit's citations are the source of truth; this session verified execution logic, not the underlying tax-law research itself, for 8 of 9 countries — AU was hand-verified end-to-end because implementing its fix required tracing the bug).

Risky: (1) A real regression was caught during browser testing, not code review: replacing the delete buttons' text content with an SVG child broke the existing `e.target.dataset.X` event-delegation pattern across all four delete handlers (jobs/bills/goals/shifts), since clicking the icon sets `e.target` to the SVG, not the button. Fixed by switching all four to `e.target.closest("[data-X]")?.dataset.X`. This is exactly the kind of bug that only surfaces by actually clicking the button, not by reading the diff — flagging so the Breaker specifically re-tests every delete affordance, not just the ones that looked obviously fine on paper. (2) The Netherlands' arbeidskorting is implemented as a simplified single-slope plateau+phase-out approximation, not the real multi-segment build-up/plateau/phase-out formula — disclosed in a code comment, consistent with the app's own "effective-rate estimate" framing, but a real simplification worth knowing about if NL accuracy is ever revisited. (3) Full manual verification (hand-traced Node execution against known-correct values) was done for all 9 countries' bracket math; the "stale figure" corrections for UK/IE/US/CA/ES were taken from the audit as given rather than independently re-researched.

Open: None blocking — all `ARCHITECTURE.md` Open items were resolved before this build started. Recommend the Breaker specifically: (a) re-verify every delete button (jobs/bills/goals/shifts) given the event-delegation bug found and fixed here, (b) spot-check at least 2-3 of the 9 country tax calculations against `TAX-ACCURACY-AUDIT.md` independently, not just trust this handover's claim, (c) test the goal-expiry boundary condition (a goal exactly at its expiry point) and the import-rejection path with a genuinely malformed file, (d) confirm the deploy-infra check still passes (no build step was added, so GitHub Pages' existing legacy branch-deploy config should still be correct, but this should be verified fresh per `breaker-protocol`'s deploy-infra-readiness section, not assumed unchanged).

Touched: `index.html` (all 6 SCOPE features), `TAX-ACCURACY-AUDIT.md` (added the independently-discovered AU structural bug), `HANDOVER.md` (this entry). No new dependencies added — icons are inlined SVG strings, no package added.

## Session 2026-07-06 (fix cycle — breaker's 3 blockers + BUG-4 resolved)

Done: Breaker's QA-REPORT.md found 3 blockers and 1 major issue against a running instance of the v9 build; all four fixed and re-verified live in-browser (headless Chrome via CDP, plus manual click-testing and console-level checks), not just by reading the diff. BUG-1 (Pace panel rendering literal "NaN" when net≤0 with a shift logged): the Pace block had its own `if(S.shifts.length)` gate, separate from and never covered by the headline/baseline/reality-bar `net>0` gate added for feature 3 — added the same guard plus an empty-state message. BUG-2 (stored XSS via job/bill/goal labels, unescaped into `innerHTML`): added an `esc()` helper and applied it at every point a user-controlled label reaches markup — three input `value=` attributes (job role, bill label, goal label), the shift-log job dropdown's option text, and both places `weekCoverage()`'s bill/goal labels flow into the coverage-bar legend (which needed escaping at the source in `weekCoverage()` since three different renderers consume its output) plus the separate `goalRows` panel which reads `goalCalc` directly. BUG-3 (shape-malformed-but-syntactically-valid localStorage crashing the app instead of degrading to seed data): `load()` only guarded `JSON.parse` throwing; added a `validShape()` check (the same one v9 already added for imports) inside `load()` itself, so both storage and import now share one shape-validation path. BUG-4 (no range validation on numeric inputs, major not blocking): added a `clamp(v,lo,hi)` helper and applied it at every read site in `model()`, `jobRate()`, and `weekCoverage()` — wage 0-10000, hours 0-168, pension 0-100%, bill/goal amounts 0-1e7, goal weeks 1-520, otherMo 0-1e7, maxDays 0-7, hoursPerDay 0-24. Chose to clamp at calculation-read time rather than rewrite the input DOM value live, since the reported bug was about nonsensical *output*, not the input box's own display, and live-rewriting during typing risks cursor-position/UX issues this fix cycle didn't need to take on.

Assumed: (1) Clamping (not rejecting with an error) is the right response to out-of-range numeric input, consistent with this app's existing "coerce via `+x||0`, never show a validation error" style throughout — a harder rejection/error-message UX would be a bigger behavior change than this fix cycle's mandate. (2) The specific range bounds chosen (e.g. wage cap $10,000/hr, weeks cap 520) are generous upper bounds meant to stop absurd/nonsensical output, not realistic real-world ceilings — flagging in case tighter bounds are wanted.

Risky: None new. All four fixes are defensive/corrective, touching read-time computation and render-time escaping, not the underlying tax logic, goal-expiry logic, or import-parsing logic verified in the prior session's HANDOVER entry — re-ran the country-tax spot checks informally during this session's testing and they still match.

Open: None. Ready for the Breaker to re-test the 3 blockers + BUG-4 specifically (per the re-test rule: failed items plus anything the fix touched, not the full suite again).

Touched: `index.html` (BUG-1 through BUG-4 fixes), `HANDOVER.md` (this entry). No new dependencies.

## Session 2026-07-06 (re-test — verdict SHIP-READY)

Done: Breaker re-tested the 3 blockers plus BUG-4 live against commit `144b4c1`, per the re-test rule (failed items only, not the full suite). All four confirmed fixed with no regressions; specifically cross-checked that `esc()`'s escaping doesn't mangle normal labels with `&`/`'` characters, and that `validShape()` and `backfillGoals()` compose correctly together. `QA-REPORT.md` verdict updated NOT READY → SHIP-READY. Deploy-infra re-checked against this exact commit, not assumed unchanged — the live GitHub Pages URL is confirmed serving `144b4c1`.

Assumed: Nothing new.

Risky: Two non-blocking documentation/consistency notes carry forward, not fixed this cycle since neither is a SCOPE criterion failure: ARCHITECTURE.md's Pages-table doesn't list the pace-panel guard against its own component breakdown, and the Take-home summary's gross/hr and net/hr fields show a clamped-sane figure rather than an empty-state message when net≤0. Both are candidate v10 polish, not defects.

Open: This is the human's Ship checkpoint. The code has been continuously pushed to `main` throughout this cycle (GitHub Pages auto-serves from `main:/`, so each push was effectively live) — meaning the XSS bug (BUG-2) was briefly live in production between the "Build v9" push and the "Fix cycle" push, for a low-traffic single-user tool with no backend/shared data. Worth the human's awareness, not an action item now that it's fixed and re-verified live.

Touched: `QA-REPORT.md` (breaker, delta update), `HANDOVER.md` (this entry).

## Session 2026-07-06 (retroactive full copywriter pass)

Done: Ran `noxus-copywriter` against the entire app for the first time — it had never actually gone through this seat before, across v6 through v9. Found and fixed a real hard-rule violation: em-dash characters throughout the copy (title, header lede, several section labels, empty-state messages, the footer disclaimer). Every em-dash replaced with a comma, colon, or period depending on context, no meaning lost. Rewrote the header lede and footer disclaimer in full (both flagged high-stakes, both delivered with an alternate; the footer's alternate was chosen — "Nothing leaves your browser, ever."). Applied two small consistency rewordings across the v9 empty-state messages so they all read as one family ("Add a job with an hourly rate to see...").

Assumed: The alternate (not main) version was chosen for the footer disclaimer as a judgment call on tone, not a mechanical fix — flagging in case a different call is wanted.

Risky: None. Copy-only pass, no logic/data/CSS touched, confirmed by the copywriter's own scope discipline and spot-checked.

Open: None.

Touched: `index.html` (copy only), `HANDOVER.md` (this entry).

## Session 2026-08-12

Done: Drafted `PROJECT.md`, a new version-independent foundational definition (problem, solution, audience, strategy, money, disconfirming evidence, reversibility). Grounded strictly in existing repo docs — every factual claim traces to SCOPE.md, LANDSCAPE.md, PARKING.md, README.md or shipped `index.html`, and is labelled [Observed]/[Inference]/[Speculation]. Verified against source: the "Nothing leaves your browser, ever." footer copy, the Door supervisor/Security guard/Visa extension seed data, and that Marlene Asare is the ONLY external user named anywhere in the repo (grep across all .md files). No code touched.

Assumed: That `PROJECT.md` is the right filename and tier for this document — it sits alongside SCOPE.md/ARCHITECTURE.md as human-owned governance, but CLAUDE.md's read-only list (order 4) does not currently name it. If the human accepts it, that list should be amended to include PROJECT.md; the Builder has not amended CLAUDE.md itself.

Risky: The strategic conclusions (audience choice, "distribution is the constraint", the 45%/15% confidence figures) are the Builder's reasoning, NOT scoped or human-approved requirements. They are argument, not instruction — nothing in PROJECT.md should be treated as authorising work. The confidence percentages are calibrated judgement, not measurement, and are labelled as such in the doc. Also: PROJECT.md asserts n=1 external users based on repo evidence only; if the human has had user conversations that never reached a repo document, §0's premise is wrong and the doc needs revising.

Open: (1) Does the human accept PROJECT.md as-is, edit it, or reject the framing? It is human-owned from acceptance onward. (2) The seed-data decision (personal vs. neutral) is still open from v9's NOT-doing list and is now the first item in PROJECT.md's strategy — it needs the human's call before it can be scoped into v10. (3) Nothing in PROJECT.md is scoped: v10 still has no SCOPE.md and no freeze.

Touched: `PROJECT.md` (new), `HANDOVER.md` (this entry). No new dependencies.

## Session 2026-08-15

Done: Drafted `REQUIREMENTS.md`, a new PRE-SCOPE document answering "what would a full application need to contain, including the legally necessary parts." Structure: current shipped feature inventory (§1, from TECH-PACK.md + SCOPE.md v9), filtered best-of-competitor list (§2, from COMPETITION-UK.md), a per-feature legal trigger map (§3, the core of the document), full feature map with legal tier coding (§4), connections/integrations map (§5), tradeoffs, a three-phase cut-line recommendation (§7), second-order effects, disconfirming evidence, decision score, confidence breakdown. Original web research this session against primary sources: FCA PERG 17 (debt counselling, fetched and read directly), FCA/PSRs AISP registration and the agency model, s21 FSMA financial promotions, the FCA's 2022 EWA position, ICO data protection fee tiers, DUAA 2025 s.164A complaints duty, ICO Children's Code scope, Equality Act 2010 digital accessibility, Online Safety Act 2023 user-to-user base duties. No code touched. Did NOT modify SCOPE.md, ARCHITECTURE.md, PARKING.md or CLAUDE.md.

Assumed: (1) That `REQUIREMENTS.md` is the right filename and tier — it is intended as human-owned pre-scope input sitting upstream of SCOPE.md, same tier as PROJECT.md. CLAUDE.md's read-only list (order 4) names neither PROJECT.md nor REQUIREMENTS.md; if the human accepts this doc, that list needs amending. The Builder has not amended CLAUDE.md. (2) That the human wanted a candidate requirements map rather than a scope — the request said "then we can move ahead with the scoping," which I read as this document being upstream of, not identical to, SCOPE.md v10. If a frozen scope was actually wanted, this is the wrong artefact. (3) That the audience and design commitments in PROJECT.md still hold; §4 and §7 are built on them.

Risky: (1) THE LEGAL ANALYSIS IS NOT LEGAL ADVICE. It is sourced and labelled, but sourced is not advised. The §3.6 debt-counselling reading is the highest-stakes item and is rated 65% confidence — PERG is FCA guidance, not law, and the information/advice boundary is fact-sensitive. It should go to a solicitor before anything in §4 group B ships. (2) The §3.6 finding has a genuine architectural consequence (feature B8: credit-agreement bills modelled as a distinct type the app never ranks) that is cheap now and expensive to retrofit — if the human agrees, it belongs in ARCHITECTURE.md as a constraint, which is the architect's file, not the Builder's. (3) The whole document's strategic recommendation contradicts its own existence: PROJECT.md §0 says the constraint is distribution, not features, and §12 of REQUIREMENTS.md admits the analysis converges suspiciously on the cheapest, most comfortable answer. (4) §2's best-of-competitor list inherits COMPETITION-UK.md's admitted weakness — Tier A competitor features are vendor-description-deep, nobody has installed one. (5) The DMCCA 2024 subscription-regime commencement detail is marked [Unknown] and must be verified before any paid tier is priced.

Open: (1) Does the human accept `REQUIREMENTS.md` as pre-scope input, and does it become human-owned on acceptance? (2) Does the phase 1 / phase 2 / phase 3 cut-line in §7 hold, and is the gate still "20 real users with week-two return" as PROJECT.md §4 set it? (3) Should B8 + E3 (credit-agreement bill typing + explicit not-debt-advice copy) be pulled forward into a v10 scope independently of everything else, given they are the identified leverage point? E3 is visitor-facing copy and routes through `noxus-copywriter`, not the Builder. (4) Is a solicitor review of §3.6 and §3.5 in budget, and does it gate v10 or only gate phase 2? (5) v10 still has no SCOPE.md and no freeze. Three carried items remain unresolved from 2026-08-12: seed-data personal-vs-neutral, PROJECT.md acceptance, and the CLAUDE.md read-only list amendment.

Touched: `REQUIREMENTS.md` (new), `HANDOVER.md` (this entry). No code modified. No new dependencies.

## Session 2026-08-15 (second entry — v10 scope candidate)

Done: Human accepted REQUIREMENTS.md's framing and asked for a PWA as a test application with feedback/data gathering. Confirmed via research that cookieless, no-persistent-identifier analytics (Plausible-style) falls outside PECR reg 6 and needs no consent banner; established that the controller trigger here is the FEEDBACK INBOX (real names, real financial situations), not the analytics, so ICO registration was already unavoidable. Human chose "Both" (local user-sent stats AND cookieless analytics) and all four decision questions. Drafted `SCOPE-v10-CANDIDATE.md`: 6 features — PWA/installable/offline, local usage stats shown to the user, a user-initiated feedback route carrying those stats plus fixed questions, cookieless analytics, the compliance surface (privacy notice, s.164A complaints route, ICO registration, 18+ statement), and credit-agreement bill typing with the not-debt-advice boundary. Explicit NOT-doing list and five open questions for the human. No code touched. Did NOT modify SCOPE.md, ARCHITECTURE.md, PARKING.md or CLAUDE.md.

Assumed: (1) That a SCOPE CANDIDATE in a separate file is the correct way to serve "move ahead with the scoping" without violating standing order 4 — SCOPE.md is human-owned and untouched; the human edits and freezes it themselves. If they expected SCOPE.md itself to be written, this is the wrong artefact and the boundary needs re-agreeing. (2) That feature 6 (credit-agreement typing) belongs in v10 rather than later — this was MY judgement from REQUIREMENTS.md §7, not the human's request, and it is flagged as open question 1 for exactly that reason. (3) That "test application" means the existing app made installable, not a separate build or a cut-down variant. (4) That analytics means a hosted third party rather than self-hosted; self-hosting changes the connections picture and was not specified.

Risky: (1) SERVICE WORKER STALE CACHE is the top technical risk. A single-file app behind a service worker can pin users to an old version permanently, and the update strategy is unspecified — this is an architect decision, called out in feature 1's criteria but not solved. Getting it wrong on a financial planning tool means users running superseded tax maths with no way to know. (2) SEED DATA IS ARGUABLY A BLOCKER, not just an open item: v10's whole purpose is putting the app in strangers' hands, and every stranger's first screen is currently the builder's own jobs and visa goal. Shipping v10 without resolving it undermines features 1 and 3. Carried unresolved since 2026-07-06. (3) The analytics feature is the only thing in this version pulling toward controller obligations, and it answers part of one question out of four — it is the most cuttable feature and the human chose it anyway; flagged as open question 3. (4) Feature 4's "no personal or financial data in network traffic" criterion needs breaker verification by actual traffic inspection, not Builder assertion. (5) Feedback destination being a personal WhatsApp/email published in a privacy notice is a real-world exposure the human has not yet decided on. (6) Legal analysis underpinning features 5 and 6 remains non-advice, with §3.6's debt-counselling reading at 65% confidence and still unreviewed by a solicitor.

Open: (1) All five open questions in SCOPE-v10-CANDIDATE.md need human answers before freeze: feature 6 in-or-out, seed data, whether analytics is worth its obligations, ICO registration identity, feedback destination. (2) Nothing is frozen — no build may begin. (3) Copy for features 3 and 6 routes through `noxus-copywriter`, not the Builder (standing order 11). (4) Feature 6's final criterion asks the ARCHITECT to record the art. 39E/PERG 17 constraint in ARCHITECTURE.md's decisions log — Builder cannot do this. (5) Carried from prior sessions and still unresolved: PROJECT.md acceptance, REQUIREMENTS.md acceptance, and the CLAUDE.md read-only list amendment to name both.

Touched: `SCOPE-v10-CANDIDATE.md` (new), `HANDOVER.md` (this entry). No code modified. No new dependencies (Plausible-or-equivalent is proposed in scope, not installed).

## Session 2026-08-19

Done: (1) Created the `shift-planner-copywriter` skill, saved to the account, modelled on `sporelo-copywriter`'s domain-restricted pattern — it names why it exists separately from `noxus-copywriter`, reads a copy deck rather than duplicating voice rules, and carries the art. 39E RAO / PERG 17 no-advice boundary as Rule 0 in the skill itself (deliberately NOT in the deck: a legal rule with two homes goes stale in one of them). (2) Created `docs/design/COPY-DECK.md` as the voice source of truth — reader profile, three voice rules, hard rules, register-per-surface map, six resolved decisions (C1-C6), word swaps, anti-patterns. (3) Drafted `docs/governance/SCOPE-v11-CANDIDATE.md`: 13 features merging frozen v10 (minus analytics per D3) with the v11 spec's end-to-end model work, ordered so data-shape changes precede readers, plus the Emma-informed visual direction (D6) and seven open questions. (4) Reorganised the repo into `docs/{governance,process,research,design,archive}` with `docs/FILING.md` documenting the rule; root reduced from 18 files to 7. Used `git mv` for tracked files so history survives. (5) Routed copy references to the new seat in `PROMPTS.md` and the v11 candidate. (6) Added a project-documentation section to README.md. No app code touched.

Assumed: (1) That SCOPE.md v10 as it currently stands (frozen 18 Aug) is the real frozen scope and my v11 candidate correctly carries it forward minus analytics — the frozen v10 still CONTAINS feature 4 (cookieless analytics) while decision D3 of the same date cuts it, so the frozen scope and the decisions log contradict each other and I resolved in favour of D3. If that is backwards, feature 4 needs restoring to the v11 candidate. (2) That `docs/` reorganisation is wanted rather than root-flat — cross-references throughout the repo are bare filenames, not paths, so nothing broke, but a session expecting files at root will not find them until it reads README or FILING. (3) That archiving rather than deleting is correct for LANDSCAPE.md, SCOPE-v10-CANDIDATE.md and v9's QA-REPORT.md. (4) That the copy deck belongs in `docs/design/` rather than root, unlike Sporelo where it sits at repo root — the skill's path reference points at `docs/design/COPY-DECK.md` accordingly. (5) That "modelled after the general noxus one" meant structurally, not voice-wise — the two voices are deliberately opposite and the skill says so.

Risky: (1) I ASSERTED EARLY IN THIS SESSION THAT `noxus-copywriter` DID NOT EXIST. That was wrong. It was not in the loaded skill list at the time and is not in the on-disk skills cache I searched, but it exists in the account and appeared later in the session. Nothing was built on the false premise, but the searching method was inadequate and the same failure would recur. (2) THE v10 FREEZE IS UNCOMMITTED AND UNRECORDED. `SCOPE.md` shows `FROZEN: 2026 - 08 - 18` but the change sits in the working tree only, nothing since 3b1a9bf is committed, and `SCOPE-HISTORY.md` has no v10 line despite its own append-only convention requiring one. If the working tree is lost the freeze is lost. (3) v10 was frozen with all five of its "Open questions before freeze" still unanswered inside the document — `PROMPTS.md` §1 explicitly warns that a prompt ending with unanswered Opens does not advance. Four of those five are still open now. (4) `SCOPE.md` v10's header comment is stale: it reads "Approved and frozen by the human 2026-07-06. Supersedes v8's SCOPE.md" which is v9's header. The freeze date format `2026 - 08 - 18` is also non-standard. I did not fix either — SCOPE.md is read-only to the Builder. (5) `CLAUDE.md` standing order 11 still routes copy to `noxus-copywriter`; for this repo it should now name `shift-planner-copywriter`. I did not edit CLAUDE.md — proposed change only, human's call. (6) The v11 candidate is 13 features plus a migration, which is a rebuild rather than a version; sizing risk is flagged in the candidate itself. (7) README.md contains an emoji and an em-dash, both hard-rule violations under the new copy deck, and states "8 countries" where the app supports 9. Flagged, not fixed — visitor-facing copy is not the Builder's to rewrite.

Open: (1) All seven open questions in `SCOPE-v11-CANDIDATE.md` need answers before freeze, most importantly: rate-deletion behaviour (feature 3), what "hours already committed" means (feature 7), and Engineer vs Guide visual direction. (2) Does the v11 candidate correctly drop analytics, or does frozen v10 feature 4 stand? See Assumed (1). (3) Should `CLAUDE.md` order 11 be amended to name `shift-planner-copywriter`, and should its read-only list be extended to name `PROJECT.md` and `REQUIREMENTS.md` (carried unresolved since 12 Aug)? (4) `SCOPE-HISTORY.md` still needs its v10 line appended — human's file, human's convention. (5) Nothing is frozen for v11 and no build may begin. (6) `ICO-DECISION.md` (PROMPTS.md prompt 1a) has not been run; it blocks the privacy notice.

Touched: `docs/design/COPY-DECK.md` (new), `docs/governance/SCOPE-v11-CANDIDATE.md` (new), `docs/FILING.md` (new), `README.md` (modified), `docs/process/PROMPTS.md` (copywriter refs), and moves: PROJECT/REQUIREMENTS/SCOPE-HISTORY to docs/governance, PROMPTS to docs/process, COMPETITION-UK/TAX-ACCURACY-AUDIT to docs/research, TECH-PACK/SPEC-v11-TARGET to docs/design, QA-REPORT to docs/archive/v9, LANDSCAPE/SCOPE-v10-CANDIDATE to docs/archive/superseded. Skill `shift-planner-copywriter` saved to the account (not in the repo). No app code modified. No new dependencies.

## Session 2026-08-19 (second entry — v11 decisions)

Done: Human answered the four blocking questions; recorded as D7-D10 in `SCOPE-v11-CANDIDATE.md` and the document updated throughout. D7 splits scope: v11 = features 1,2,3,4,7,8,9,10,13 (model rebuild + PWA + copy pass), v12 = features 5 (second currency), 6 (protected time), 11 (local stats + feedback route). D8 resolves rate deletion by snapshotting rate name and value onto each shift at log time, so history never references the rate card and deletion becomes a non-event. D9 defines "hours already committed" as shifts logged for the current week including ones dated later in that week. D10 selects Guide over Engineer, decided by rendering the headline screen in both and comparing. Also caught and corrected a dependency error I had stated in conversation: the compliance surface is triggered by COLLECTING personal data, not by public use, so deferring the feedback route (feature 11) defers the privacy notice, ICO registration and s.164A complaints route with it — but the not-debt-advice statement, the tax disclaimer and WCAG 2.2 AA are triggered by the activity rather than by data and ship in v11. Feature 12 therefore splits rather than moving as a block. No app code touched. SCOPE.md, ARCHITECTURE.md, PARKING.md and CLAUDE.md untouched.

Assumed: (1) That the not-debt-advice statement must ship in v11 alongside feature 4's credit-commitment typing rather than deferring with the rest of compliance — this is my reading of `REQUIREMENTS.md` §3.6, not the human's instruction, and it adds a copy deliverable to v11 that the split did not explicitly include. (2) That `SPEC-v11-TARGET.html` remains useful as a layout and flow reference while its Engineer tokens are discarded — the alternative reading is that the whole document is superseded by the Guide decision. (3) That feature 13's copy pass covers only v11 strings and v12 gets its own pass. (4) That deferring feature 5 does not strand any part of feature 4 — dated obligations and second-currency obligations were drafted as separate concerns, but they were drafted together and the separation has not been tested against the data model.

Risky: (1) I STATED IN CONVERSATION THAT v11 "CANNOT BE PUBLICISED TO ANYONE" WITHOUT THE COMPLIANCE SURFACE. That was wrong and the human caught it. The corrected position is in the candidate now, but the error is the second factual overstatement I have made this session (the first being `noxus-copywriter` not existing) and both were confident assertions rather than flagged uncertainties. (2) The line between "informal conversation with a colleague" and "a published feedback channel that makes you a controller" is a spectrum, not a rule, and the candidate states it as guidance rather than a bright line because there is no bright line to state. (3) NO GUIDE DESIGN TOKEN DOCUMENT EXISTS. It is the `noxus-design-system` skill's default deliverable and the architect pass needs it as an input; the architect will otherwise improvise tokens, which is exactly the drift the design system exists to prevent. This blocks prompt 2. (4) The warm-vs-cool accent question is unresolved and is the most likely place for silent drift, because a cool accent is what the human's reference actually looks like while the style file specifies warm. (5) Feature 9's v11 shape may now be smaller than drafted, since second currency left — it has not been re-scoped and may be overstated in the candidate.

Open: (1) Warm or cool accent within Guide, decided in the architect pass and recorded with its reason. (2) The Guide token document must be produced before prompt 2 runs. (3) Feature 2's validation rules are unstated and the Builder must not invent them. (4) Does feature 9 need re-scoping now that second currency is deferred? (5) Carried, unresolved across three sessions now: `PROJECT.md` acceptance, `REQUIREMENTS.md` acceptance, `CLAUDE.md` read-only list amendment, `CLAUDE.md` order 11 renaming to `shift-planner-copywriter`, and the missing v10 line in `SCOPE-HISTORY.md`. (6) v11 is not frozen. No build may begin.

Touched: `docs/governance/SCOPE-v11-CANDIDATE.md` (D7-D10 recorded, feature 3 and 7 criteria rewritten, v11/v12 routing table added, compliance split explained, visual direction resolved, open questions pruned), `HANDOVER.md` (this entry). No app code modified. No new dependencies.
